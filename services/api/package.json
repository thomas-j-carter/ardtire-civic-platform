{
  "name": "@ardtire/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -s build && node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome lint .",
    "test": "echo \"no tests yet (api scaffold)\""
  },
  "dependencies": {
    "hono": "^4.6.12",
    "@hono/node-server": "^1.13.0",
    "yaml": "^2.5.1",
    "jose": "^6.0.11"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^5.0.0"
  }
}

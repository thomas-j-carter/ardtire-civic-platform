# @ardtire/config

Centralized shared configuration for the monorepo.

This package will host shared presets and base configs for:
- TypeScript (`tsconfig` presets)
- Tailwind (design tokens + presets)
- Tooling conventions used by apps and packages

It is intentionally lightweight and private.
